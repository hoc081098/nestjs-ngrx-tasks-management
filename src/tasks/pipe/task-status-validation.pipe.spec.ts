import { TaskStatusValidationPipe } from './task-status-validation.pipe';

describe('TaskStatusValidationPipe', () => {
  it('should be defined', () => {
    expect(new TaskStatusValidationPipe()).toBeDefined();
  });
});
